version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.17.1
   
    commands:
        # install npm
        - npm install
       
  build:
    commands:
        # run build script
        - npm run build
     
artifacts:
  files: '**/*'
  discard-paths: yes